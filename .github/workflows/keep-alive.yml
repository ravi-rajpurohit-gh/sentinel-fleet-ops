name: Keep Streamlit App Alive

on:
  schedule:
    # Runs twice daily at 08:00 and 20:00 UTC to prevent Streamlit Community Cloud sleep
    - cron: '0 8 * * *'
    - cron: '0 20 * * *'
  workflow_dispatch:

jobs:
  ping:
    name: Ping Streamlit App
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Ping sentinel-fleet-ops.streamlit.app
        run: |
          # -L follows redirects — critical because Streamlit returns 303 when sleeping,
          # pointing to a wake-up page. Without -L the ping exits on the redirect and
          # the app never actually wakes up.
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 60 https://sentinel-fleet-ops.streamlit.app/)
          echo "HTTP status: $STATUS"
          if [ "$STATUS" -lt 200 ] || [ "$STATUS" -ge 400 ]; then
            echo "Unexpected status code: $STATUS"
            exit 1
          fi
          echo "App is alive."
